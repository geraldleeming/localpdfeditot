{
  "name": "local-pdf-editor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A PDF editor that runs entirely in your browser. No server, no uploads.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "fixture": "node test/make-fixture.mjs",
    "test": "npm run fixture && vite build && node test/smoke.mjs && node test/pages-check.mjs && node test/privacy-check.mjs",
    "verify:privacy": "npm run fixture && vite build && node test/privacy-check.mjs"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.3.289"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "playwright-core": "^1.62.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
